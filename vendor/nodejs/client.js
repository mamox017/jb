const btn = document.getElementById('search');

btn.addEventListener('click', (event) => {
	event.preventDefault();
	const job = document.getElementById('query').value;
	console.log(job);
	//Search bar algorithm
})

function searchJobs() {

}